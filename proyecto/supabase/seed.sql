-- Seed: Create admin user (run this in Supabase SQL Editor)
-- Email: admin@admilico.com / Password: admin123

-- Note: The trigger on_auth_user_created will auto-create the profile
-- Sign up via the app UI at /login or use the Supabase Auth API

-- After signing up, update the role to admin:
-- UPDATE public.profiles SET role = 'admin' WHERE email = 'admin@admilico.com';
