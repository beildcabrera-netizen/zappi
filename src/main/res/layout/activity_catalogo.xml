<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:paddingTop="16dp"
    android:paddingBottom="8dp"
    android:background="@android:color/white"
    tools:context=".CatalogoActivity">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="RAPI ZAPI Catálogo"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_gravity="center_horizontal"
        android:textColor="#3F51B5"
        android:layout_marginBottom="16dp"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginBottom="8dp">

        <Spinner
            android:id="@+id/spinner_categoria"
            android:layout_width="0dp"
            android:layout_height="40dp"
            android:layout_weight="1"
            android:layout_marginEnd="4dp"
            android:entries="@array/categorias_calzado"
            android:background="@drawable/spinner_background" />

        <Spinner
            android:id="@+id/spinner_talla"
            android:layout_width="0dp"
            android:layout_height="40dp"
            android:layout_weight="1"
            android:layout_marginEnd="4dp"
            android:entries="@array/tallas_calzado"

            android:background="@drawable/spinner_background" />

        <Spinner
            android:id="@+id/spinner_color"
            android:layout_width="0dp"
            android:layout_height="40dp"
            android:layout_weight="1"
            android:entries="@array/colores_calzado"
            android:background="@drawable/spinner_background" />
    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal"
        android:orientation="horizontal"
        android:layout_marginBottom="12dp">

        <Button
            android:id="@+id/button_aplicar_filtros"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="APLICAR FILTROS"
            android:backgroundTint="#FF9800"
            android:textColor="@android:color/white"
            app:cornerRadius="20dp"
            android:paddingStart="24dp"
            android:paddingEnd="24dp"
            android:layout_marginEnd="16dp"/>

        <ImageButton
            android:id="@+id/button_carrito"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:src="@drawable/carritoo"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Carrito de compras"
            android:layout_gravity="center_vertical"
            android:scaleType="centerInside"/>

    </LinearLayout>

    <EditText
        android:id="@+id/edit_text_busqueda"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Buscar Zapatos..."
        android:inputType="text"
        android:padding="12dp"
        android:textSize="16sp"
        android:background="@drawable/spinner_background"
        android:drawableStart="@android:drawable/ic_search_category_default"
        android:drawablePadding="8dp"
        android:layout_marginBottom="12dp"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view_productos"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:scrollbars="vertical"/>

</LinearLayout>

